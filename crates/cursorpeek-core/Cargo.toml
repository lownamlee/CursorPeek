[package]
name = "cursorpeek-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.1"
description = "Platform-neutral parsing and validation core for CursorPeek"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
encoding_rs = { version = "=0.8.35", default-features = false, features = ["alloc"] }
image = { version = "=0.25.10", default-features = false }
